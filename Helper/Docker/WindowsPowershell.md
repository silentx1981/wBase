# PowerShell commands

## Start Docker Service
* Open Powershell
* Click ```wsl```
* Change to the wBase directory ```cd /path/to/wBase```
* Run the following command to start the Docker service:
```docker compose up -d```